# CLAUDE.md — 卒業研究プロジェクトの憲法

このファイルは Claude Code がこのリポジトリで作業するときに必ず読むものです。
**新しいルールを追加したくなったら、まずここに書いてください。**

---

## プロジェクト概要

- **テーマ**：等角予測（conformal prediction）による分布によらない予測区間
- **所属**：東京理科大学 理学部第一部 応用数学科／黒沢研究室
- **年度**：2026年度（R08）後期
- **著者**：（氏名を記入）
- **提出形態**：卒業論文（LaTeX）＋発表スライド＋プログラム一式を BOX に提出

このリポジトリは**卒業論文の実験パート**を管理します。論文本文（TeX）は `thesis/`、
提出用のパッケージングは `submission/` を参照してください。

---

## 絶対に守ること（違反はバグ扱い）

### 1. 共形分位点の定義

分位点は必ず

```
k = ceil((n + 1) * (1 - alpha))     # n はキャリブレーション集合のサイズ
q_hat = sort(scores)[k - 1]         # k > n のときは +inf
```

で計算する。**共形分位点の計算において**以下は禁止：

- `np.quantile(scores, 1 - alpha)` — 線形補間が入り有限標本被覆保証が壊れる
- `ceil(n * (1 - alpha))` — off-by-one。n が大きいと差が見えず気づけない

実装・修正したら必ず `tests/test_quantile_index.py` を通すこと。

**記述統計としての分位点には `np.quantile` を使ってよい。** 層別の境界を切る、
テスト集合の残差の分位点を参考値として出す、といった用途は共形分位点とは別物である。
ただし**その行に「共形分位点ではない」旨のコメントを必ず付ける**こと。
コメントの無い `np.quantile` はレビューで差し戻す。

### 2. データリークの禁止

キャリブレーション集合は学習に一切使わない。データ分割は必ず乱数シードで管理し、
分割の再現性を保証する。`tests/test_split_leakage.py` がこれを検査する。

### 3. 再現性

- すべての実験スクリプトは `--seed` 引数を取る
- 同じ seed で2回実行したら `results/` の csv が完全に一致すること
- `np.random.seed` ではなく `np.random.default_rng(seed)` を使う（グローバル状態を汚さない）
- 実験結果は必ず `results/*.csv` に保存する。図は csv から作る。
  **実験を回し直さずに図だけ直せる状態を保つこと**（締切前に効く）
- 図も再現的であること。PDF に埋め込まれる作成時刻などのメタデータを落とし、
  同じ csv から2回作った図が pdf・png ともバイト単位で一致すること。
  図は git 管理の提出物なので、内容が同じなら差分が出てはいけない

### 4. 報告の作法

**被覆率を点推定だけで報告するのは禁止。必ず不確実性を併記する。**
ただし何のばらつきを見たいかで使う区間が変わるので、下表で選ぶこと。

| 見たいばらつき | 使う区間 | 使う場面 |
|---|---|---|
| **1試行内**（有限のテスト点による二項変動） | Clopper–Pearson | 1回の適用の被覆率を報告するとき（実データ解析など） |
| **試行間**（較正集合の引き直し） | 平均 ± 1.96 × 試行間SD / sqrt(R) | 多数回試行のシミュレーション（E1, E2, E5, E6） |
| **学習集合間**（学習データの引き直し） | rep 間の SD から作る区間 | モデル比較の頑健性を見るとき（E2のC4） |
| **テスト集合間**（固定のテスト集合を全試行で共有している場合） | `p(1-p)/N_test` を分散に加える | E1, E2 のように固定テスト集合を使い回す実験 |

**これらを混同しないこと。** 試行間のばらつきを見たい場面で Clopper–Pearson を使うのは
誤り（母数が違う）。逆に1回きりの適用に試行間SEは作れない。

**成分が複数効く場合は分散を足す。** とくに固定テスト集合を全試行で共有している場合、
その集合固有のズレは全試行に共通のバイアスとして乗るため、**試行数を増やしても消えない**
（√R で割ってはいけない）。この場合の報告区間は

    SE = sqrt( 試行間SD^2 / R  +  p(1-p) / N_test )

とする。n_cal が大きいほど試行間成分は縮み、テスト集合成分が支配的になる。
どの成分を含めた区間かを必ず結果の表と本文に明記すること。

### 5. テストは実装と独立に

新しい手法を追加したら、対応する検証テストを `tests/` に追加する。
テストは**理論から導かれる性質**を検査すること（「実装がこう動くから」ではなく
「理論がこう言うから」で書く）。具体的には以下の型を使う：

- 共形 p 値が離散一様分布に従う（KS / カイ二乗検定）
- 被覆率が Beta(n+1-l, l), l = floor((n+1)*alpha) に従う
- 定数予測器のような最悪のモデルでも被覆率が 1-alpha を満たす
- 外部実装（MAPIE）と数値が一致する

---

## 実装方針

- Python 3.11。`numpy` / `scipy` / `scikit-learn` / `pandas` / `matplotlib` のみ使用
- 外部の等角予測ライブラリ（MAPIE, crepes）は**自作実装の検証用としてのみ**使用可。
  本体の実装に使ってはいけない（卒論の中身がなくなる）
- 関数には型ヒントと docstring をつける。**docstring には対応する論文の式番号を書く**
  （付録A「数式と実装の対応表」を作るため）
- `src/conformal/split.py` は論文第3章に対応する。**読んで理解できる長さを保つこと**
- コメントは日本語で書く。提出時に「他人が見て理解できるか」を黒沢先生が確認するため
  （提出要領 04. データの提出を参照）
- 各モジュールの冒頭に「卒論のどの章・どの実験に対応するか」をコメントで書く
  （これも提出要領の明示的な要求事項）

---

## コードを書くときの追加ルール

- **私が理解できない実装は却下する。** トリッキーな最適化より素直な実装を優先すること
- 実装した関数について、対応する数式を docstring に書く
- 実験スクリプトを追加したら `specs/` に対応する仕様書があることを確認する。
  なければ「仕様書を先に書いてください」と私に言うこと
- 1つの変更で複数の実験を同時に壊さないこと。変更後は `make test` を実行する

---

## ディレクトリの役割

| パス | 役割 |
|---|---|
| `src/conformal/` | 手法の実装。ここが論文第3〜4章に対応 |
| `tests/` | 理論に基づく検証テスト。**実装を信用する唯一の根拠** |
| `specs/` | 実験仕様書。実装の前にここを書く。論文第5章の下書きにもなる |
| `experiments/` | 実験の実行スクリプト。`results/` に csv を吐く |
| `results/` | 数値結果（csv）。**論文の一次資料なので git 管理する** |
| `figures/` | 図。csv から再生成できること |
| `thesis/` | 論文本文（TeX）と `refs.bib` |
| `docs/` | 設計書・提出要件・データの入手方法 |
| `submission/` | BOX 提出用のチェックリストと Readme.txt の雛形 |
| `slides/` | 発表スライド |

---

## 提出要件（黒沢研究室・R08年度）から来る制約

実装に影響する項目のみ抜粋。全文は `docs/提出要件と締切.md` を参照。

- **プログラムにはコメントを十分に書き、卒論のどこに対応するかを明記する**
- **`Readme.txt` に実行手順を書く**（`submission/Readme_template.txt` を使う）
- 集計結果は csv / xlsx で提出。xlsx なら `Readme` シートを作る
- 変数名が `x` のままなど、意味の分からないものを残さない
- 参考文献は `\bibitem` ではなく **pbibtex**（`thesis/refs.bib` を使う）
- 秘匿データ・非公開データは提出しない（本研究は公開データのみを使うので該当しない見込み）

---

## 実験の再開条件（2026年9月時点）

**第3章（定理と証明）の下書きが `thesis/` に入るまで、新しい実験を開始しない。**

- 対象：E3b、E4、E5、E6、E7
- 許可されること：既存実験の図・表の修正、バグ修正、テストの追加、仕様書の整備
- 私が新しい実験を始めようとしたら、この節を根拠に止めてください

理由：2026年9月の第1週で E1・E1b・E2・E3 が終わり、当初計画（E3 は10月下旬）より
10週間先行した。一方で本文は0ページである。実装が速いことは、そのまま
「12月に実験だけ10個あって本文が白紙」への最短経路でもある。
口頭試問で問われるのは実験の数ではなく、定理を自分で証明できるかである。

---

## 生成AIの利用について

本プロジェクトでは実装に Claude / Claude Code を利用する。以下を守ること。

- **仕様の策定・検証テストの設計・結果の解釈・本文の執筆は著者が行う**
- 生成されたコードは、上記「テストは実装と独立に」の基準で著者が検証する
- `src/conformal/split.py` の中核は、著者が内容を理解した上で自ら記述する
- 論文の付録に利用範囲と検証体制を記載する（`docs/AI利用の記載.md` に文案）
- **着手前に指導教員に利用の可否と記載方法を確認する**

---

## よくあるバグ（レビュー時の重点確認項目）

| # | バグ | 検出方法 |
|---|---|---|
| 1 | `np.quantile` を使ってしまう | `tests/test_quantile_index.py` |
| 2 | `ceil((n+1)(1-a))` を `ceil(n(1-a))` にする | 同上・被覆率のBeta検定 |
| 3 | キャリブレーション集合が学習に混入 | 定数予測器テスト |
| 4 | CQR のスコアの符号ミス（正しくは `max(q_lo - y, y - q_hi)`） | 手計算した数例との照合 |
| 5 | 同点（離散スコア）の扱い | 離散データでのテスト |
| 6 | 重み付き等角予測で重みの正規化を忘れる | 重みが全て1なら通常のsplitと一致するかテスト |
| 7 | 被覆率を全テスト点の平均だけで報告し試行間のばらつきを潰す | 生の試行別データを必ず保存 |
